import { StyledByClickDirective } from './styled-by-click.directive';

describe('StyledByClickDirective', () => {
  it('should create an instance', () => {
    const directive = new StyledByClickDirective();
    expect(directive).toBeTruthy();
  });
});
